---
type: llm
---

PASS if the tests written for `classify` cover both branches: at least one case where the
argument is even and one where it is odd.

FAIL if only one branch is exercised, or if no test targets `classify` at all.
